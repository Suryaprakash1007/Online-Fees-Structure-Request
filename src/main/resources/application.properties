# Application name
spring.application.name=fees

# Database
spring.datasource.url=${DB_URL:jdbc:postgresql://dpg-d37bobhr0fns739cpcb0-a.singapore-postgres.render.com:5432/feesportal_db?sslmode=require}
spring.datasource.username=${DB_USERNAME:feesportal_db_user}
spring.datasource.password=${DB_PASSWORD:u6kaKsj0AoMYNa0lPYvfJTzvtYpvKNij}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server port (Render provides $PORT)
server.port=${PORT:8081}

# Mail
spring.mail.host=smtp.sendgrid.net
spring.mail.port=2525
spring.mail.username=apikey
spring.mail.password=${SENDGRID_API_KEY}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.from=ishankishen454545@gmail.com

# File upload
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB
